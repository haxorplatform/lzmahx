#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../peerjs-server/bin/peerjs" "$@"
  ret=$?
else 
  node  "$basedir/../peerjs-server/bin/peerjs" "$@"
  ret=$?
fi
exit $ret
